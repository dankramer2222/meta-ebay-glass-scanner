# iOS integration placeholder

Create a SwiftUI iOS app named MetaEbayScanner, add Meta's current Wearables Device Access Toolkit from the official Meta developer repository, then connect these layers:

`Meta camera -> ScannerAPI -> eBay result -> Meta Display`

Keep Meta SDK-specific code in a single adapter so SDK API changes do not affect the rest of the app.
